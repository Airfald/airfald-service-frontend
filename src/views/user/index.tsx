import React, {Component} from 'react';
import './index.scss';

const UserList: React.FC<any> = props => {
  return (
    <div className='user'>
      user
    </div>
  );
};

export default UserList;
